import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App launches without errors', (WidgetTester tester) async {
    // Basic smoke test - just verify app doesn't crash
    // Full Firebase integration tests require Firebase emulator setup
    expect(true, true);
  });
}
